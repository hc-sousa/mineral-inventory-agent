from django.urls import path
from . import views

app_name = 'document_store_agent'

urlpatterns = [
    # Define URL patterns for document store agent
]
from django.urls import path
from . import views

app_name = 'document_store_agent'

urlpatterns = [
    # Define URL patterns for document store agent
]
